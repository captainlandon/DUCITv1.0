package com.ducit.launcher.ui.theme

import androidx.compose.ui.graphics.Color

// Design tokens for the v0.1 offline shell. Dark-first: the reference UI
// (Ducit home mockup) is a near-black navy ground with a single blue
// accent family — no gradients or decoration are load-bearing here, this
// is the launcher fallback, not the Now/Command surface.
val DucitBackground = Color(0xFF05070C)
val DucitSurface = Color(0xFF10141F)
val DucitSurfaceVariant = Color(0xFF1A2030)
val DucitOnBackground = Color(0xFFE8EBF2)
val DucitOnSurfaceMuted = Color(0xFF8B93A7)
val DucitAccent = Color(0xFF4C8DFF)
val DucitAccentMuted = Color(0xFF2A3A5C)
val DucitDivider = Color(0xFF232A3B)
